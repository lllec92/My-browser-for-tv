<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootOverlay"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_root">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Tab bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/tab_bar_height"
            android:background="@color/bg_tabbar"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="@dimen/tv_overscan_margin"
            android:paddingEnd="16dp">

            <HorizontalScrollView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:scrollbars="none">

                <LinearLayout
                    android:id="@+id/tabBarContainer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal" />

            </HorizontalScrollView>

            <ImageButton
                android:id="@+id/newTabButton"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/new_tab"
                android:focusable="true"
                android:padding="8dp"
                android:src="@android:drawable/ic_input_add" />

            <ImageButton
                android:id="@+id/bookmarksButton"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/open_bookmarks"
                android:focusable="true"
                android:padding="9dp"
                android:src="@android:drawable/ic_menu_agenda" />

            <ImageButton
                android:id="@+id/historyButton"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/open_history"
                android:focusable="true"
                android:padding="9dp"
                android:src="@drawable/ic_history" />

            <ImageButton
                android:id="@+id/settingsButton"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/settings"
                android:focusable="true"
                android:padding="8dp"
                android:src="@android:drawable/ic_menu_preferences" />

        </LinearLayout>

        <!-- Address bar + page actions -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/bg_surface"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="@dimen/tv_overscan_margin"
            android:paddingEnd="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp">

            <ImageButton
                android:id="@+id/starButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/add_bookmark"
                android:focusable="true"
                android:padding="8dp"
                android:src="@drawable/ic_star_outline" />

            <EditText
                android:id="@+id/addressBar"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/focus_highlight"
                android:hint="@string/address_bar_hint"
                android:imeOptions="actionGo"
                android:inputType="textUri"
                android:maxLines="1"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_disabled"
                android:textSize="@dimen/text_size_medium" />

            <ImageButton
                android:id="@+id/micButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/voice_search"
                android:focusable="true"
                android:padding="7dp"
                android:src="@drawable/ic_mic" />

            <ImageButton
                android:id="@+id/zoomOutButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/zoom_out"
                android:focusable="true"
                android:padding="7dp"
                android:src="@drawable/ic_zoom_out" />

            <ImageButton
                android:id="@+id/zoomInButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/icon_button_bg"
                android:contentDescription="@string/zoom_in"
                android:focusable="true"
                android:padding="7dp"
                android:src="@drawable/ic_zoom_in" />

        </LinearLayout>

        <!-- WebView tab contents -->
        <FrameLayout
            android:id="@+id/webviewStack"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <FrameLayout
                android:id="@+id/webviewContainer"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/bg_surface" />

        </FrameLayout>

    </LinearLayout>

    <ProgressBar
        android:id="@+id/loadingProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:layout_marginTop="@dimen/tab_bar_height"
        android:indeterminate="true"
        android:max="100"
        android:progressTint="@color/accent_primary"
        android:visibility="gone" />

    <!-- Mini (windowed) player overlay -->
    <FrameLayout
        android:id="@+id/miniPlayerContainer"
        android:layout_width="@dimen/mini_player_width"
        android:layout_height="@dimen/mini_player_height"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/mini_player_margin"
        android:background="@drawable/mini_player_bg"
        android:focusable="true"
        android:visibility="gone"
        android:elevation="12dp">

        <androidx.media3.ui.PlayerView
            android:id="@+id/miniPlayerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:use_controller="false"
            app:resize_mode="fit" />

    </FrameLayout>

    <!-- Cursor overlay: last child of the root so it always draws above the tab
         bar, address bar, page content, AND the mini player. Movement range is
         the whole screen (see CursorController), not just the page area. -->
    <ImageView
        android:id="@+id/tvCursor"
        android:layout_width="@dimen/cursor_size"
        android:layout_height="@dimen/cursor_size"
        android:src="@drawable/ic_cursor"
        android:alpha="0.8"
        android:elevation="32dp"
        android:visibility="visible"
        android:contentDescription="@null" />

</FrameLayout>
