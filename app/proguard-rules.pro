# Add project specific ProGuard rules here.
# Minification is disabled by default (see build.gradle.kts), so this file
# only matters if you switch isMinifyEnabled to true.
-keepattributes *Annotation*
-keep class androidx.media3.** { *; }
